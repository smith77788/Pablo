apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: tg-platform
spec:
  serviceName: postgres
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - name: POSTGRES_USER
              value: platform
            - name: POSTGRES_PASSWORD
              value: platform123
            - name: POSTGRES_DB
              value: tgplatform
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "platform", "-d", "tgplatform"]
            initialDelaySeconds: 10
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: tg-platform
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: 5432
