# Очередь задач

Обновлено: 2026-09-04

Живой список. Закрытое переносится в COMPLETED кратко, без подробностей —
подробности в истории коммитов.

## OPEN

### 🔴 ОБЯЗАТЕЛЬНО В ПЕРВУЮ ОЧЕРЕДЬ — Infragram Virtual Layer (решение владельца)

Направление владельца: Infragram — не набор модулей, а вычислительный слой
поверх Telegram. «Telegram показывает, что произошло; Infragram строит модель
того, что происходит». Событие → интерпретация → состояние → решение → действие.

**Ядро СДЕЛАНО** (`services/virtual_layer.py`, схема v211, тесты
`test_virtual_layer_*`): динамическое СОСТОЯНИЕ (value + confidence + source +
распад + история) на любом уровне (user/bot/campaign/network); СИГНАЛ двигает по
лестнице воронки; распад остужает без подтверждения; КАСКАД (много «горячих»
детей → «горячий» родитель); на реальных переходах рождаются ВИРТУАЛЬНЫЕ
СОБЫТИЯ (purchase_intent_detected, user_lost_interest) в общую шину. Первый
источник сигналов подключён — сенсор намерений. Надстройка над
`unified_contacts.stage`, не второй источник правды.

**Осталось по слою (по убыванию отдачи):**
- [ ] **Ещё источники сигналов**: реакции/ответы/тишина из relay и Vault, тапы
      по кнопкам ботов, вступления/выходы. Сейчас кормит только сенсор намерений.
- [ ] **Каскад в проде**: периодический `recompute_cascade` bot←user,
      campaign←bot; состояние бота/кампании в мир-снимок и подсказки мозга.
- [ ] **Автоматизации на виртуальных событиях**: подписать существующие цепочки
      (нудж, оффер) на purchase_intent_detected / user_lost_interest вместо
      сырых условий.
- [ ] **Экран состояний** в мини-аппе: воронка сущности, уверенность, история
      переходов (temporal-сигнал «заходил и уходил»).

**Полезные механики из разбора владельца — в backlog этого направления:**
- [ ] **Intent Router**: «пиши что угодно» → определить намерение → направить к
      нужному боту/каналу (не в конкретный сценарий). 50 ботов → единый вход.
- [ ] **Automation Discovery**: находить повторяющийся ручной труд (менеджер шлёт
      одному клиенту один и тот же текст) → предлагать автоматизацию.
- [ ] **Invisible Trigger**: пользовательские триггеры, которых нет как событий
      Telegram («трижды посмотрел, не ответил») — строятся из виртуальных
      состояний этого слоя.
- [ ] **Mirror Bot / Clone Factory**: цифровой двойник бота для теста изменений
      на копии до реального трафика; массовое клонирование managed-ботов.

**Bot Mesh (боты координируют задачи) — ЯДРО СДЕЛАНО.** Telegram РЕАЛЬНО
поддерживает bot-to-bot (проверено по core.telegram.org/api/bots/bot-to-bot):
opt-in в @BotFather; в группах доставка по `/cmd@bot` или reply, в ЛС —
обоюдный opt-in, в бизнес-аккаунтах — только у отправителя. Гашение петель
Telegram НЕ делает — это на нас. Построено `services/bot_mesh.py` (схема v212):
конверт задачи + четыре независимых предохранителя (глубина, дедлайн, дедуп
шага через UNIQUE-индекс, цикл в трассе); любой в одиночку рвёт лавину.
Осталось:
- [ ] **Транспорт**: хендлеры приёма bot-message (updateNewMessage с from.is_bot /
      updateBotNewBusinessMessage) и отправка `/cmd@bot`; требует включённого
      режима в @BotFather у каждого участника — обкатать на 2 ботах.
- [ ] **Маршруты из шаблонов**: связать с `network_builder` — топология сети
      задаёт маршрут задачи (Sales→Qualification→CRM).
- [ ] **Экран Mesh**: список задач, трасса передач, брошенные с причиной.

**Границы (проверить до постройки, НЕ строить на непроверенном):**
- **Forked Reality (симуляция веток до действия)** — отдельный слой поверх
  ядра, требует симулятора; пока не начинать.

### HIGH
- [ ] **Разрезать `mini_app_api.py`** (18.6k строк, 500 маршрутов в одном файле).
      Единственная точка входа веб-поверхности и самый несвязанный модуль
      (in-degree 2). Резать по доменам, шов уже есть.
- [ ] **Координация пейсинга между процессами.** Лимиты флуда живут в памяти
      процесса, поэтому исполнитель операций может быть только один
      (`services/replica_guard.py` следит). Пока это так — горизонтально
      масштабировать нельзя.
- [ ] **Абстракция транспорта (HAL).** 47 сервисов и 9 хендлеров работают с
      Telethon напрямую; второй драйвер (Bot API, Business API) добавить некуда.
      Шов для этого поставлен — таблица исполнителей в `op_worker`.

### MEDIUM
- [ ] **Baseline схемы.** Механизм и генератор готовы
      (`deploy/scripts/make_schema_baseline.py`), сам снимок не снят: его берут с
      базы, прошедшей всю историю. План — `docs/SCHEMA_CONSOLIDATION_PLAN.md`.
- [ ] **Обкатать пакетный инвайт** (`INVITE_BULK_API=1` или `bulk_api` у
      операции). Выключен по умолчанию; проверить на части флота форму
      `missing_invitees` в проде и сравнить разбивку отказов с поштучным путём.
- [ ] **Решить судьбу `services/wb_chat/`** — модуль автоматизации чатов
      Wildberries возвращён из ветки, покрыт тестами, но в `main.py` НЕ
      подключён. Это другая площадка: либо запускаем осознанно, либо выносим.
- [ ] **`platform/apps/web`** — 41 экран Next.js остался в ветке
      `claude/telegram-bot-services-xfAh6` до 2026-09-04, затем удалён. Если
      нужен веб-дашборд, экраны берутся по SHA из `docs/BRANCH_RESTORE_POINTS.md`
      и переводятся на `mini_app_api`.

### LOW
- [ ] Ввод текста приглашения для метода «ссылка в ЛС» есть в боте и мини-аппе;
      остальные настройки инвайта в боте по-прежнему беднее, чем в мини-аппе.

## COMPLETED (кратко)

- **2026-09-04** Консолидация репозитория: возвращена работа из чужих веток
  (`.botmother`, стандарты, `wb_chat`, `budget_radar`, SSRF-гард), удалены
  дубли (`platform/`, `nodes-service/`, BASIC.FOOD), в корне появился `CLAUDE.md`.
- **2026-09-04** Модуль INVITING разобран целиком (12 коммитов).
- **2026-09-03** Структурный долг: таблица исполнителей вместо цепочки из 70
  `elif`, baseline-механизм схемы, ADR о едином ядре, 31 обход шины операций
  закрыт, мёртвый дубль ограничителя запросов удалён.
- r13–r19 и раньше — см. историю и `IMPLEMENTATION_LOG.md`.
