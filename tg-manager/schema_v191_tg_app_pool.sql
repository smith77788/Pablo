-- schema_v191_tg_app_pool.sql — пер-аккаунтное Telegram-приложение (api_id).
-- Первопричина: весь флот подключался под одной парой TG_API_ID/TG_API_HASH —
-- прямой корреляционный признак связности когорты для Telegram.
-- Колонка закрепляет выбранное приложение за аккаунтом: смена api_id у живого
-- аккаунта сама по себе палевна, поэтому назначение должно быть стабильным и
-- переживать изменение пула (services/tg_apps.py).
ALTER TABLE tg_accounts ADD COLUMN IF NOT EXISTS api_id BIGINT;
