# ── Ревизия зависимостей (сторожит tests/test_deps_freshness.py) ─────────────
# Пины — тоже стареющий актив: код не меняется, а совместимость с Telegram
# уходит сама. Гейт краснеет, когда ревизию не делали дольше DEPS_MAX_AGE_DAYS,
# чтобы отставание было ОСОЗНАННЫМ решением, а не тихим гниением.
# При ревизии: сверить актуальные версии, обновить заметку ниже, поднять дату.
# DEPS_REVIEWED: 2026-07-25
# DEPS_MAX_AGE_DAYS: 180
#
# СОСТОЯНИЕ НА 2026-07-25 (сверено с реальностью):
#   aiogram==3.30.0 — актуальный стабильный 3.x, Bot API 9.4. Ветку не менять и
#   вниз не откатывать: используется API после 3.7 (Bot(default=
#   DefaultBotProperties(parse_mode=...)), НЕ kwarg parse_mode). main.py трогает
#   приватный AiohttpSession._connector_init["ssl"] — при следующем апгрейде
#   проверить, что атрибут ещё существует.
#   telethon>=1.36.0 — намеренно НЕ жёсткий пин: клиент говорит с MTProto
#   напрямую, при смене слоя Telegram застрявшая версия отваливается первой.
aiogram==3.30.0
asyncpg==0.29.0
aiohttp==3.10.10
httpx>=0.27.0
python-dotenv==1.0.1
openai>=1.40.0
anthropic>=0.69.0
telethon>=1.36.0
PySocks>=1.7.1
qrcode[pil]>=7.4
# Pillow приходил транзитивно через qrcode[pil]; теперь это ПРЯМАЯ зависимость
# (services/avatar_factory.py рисует аватары), поэтому пин явный — иначе смена
# экстры в qrcode тихо унесла бы генератор аватаров.
Pillow>=10.0.0
aiohttp-socks>=0.7.3
pypdf>=4.3.1
python-docx>=1.1.2
openpyxl>=3.1.5
pycryptodome>=3.19.0
# opentele>=1.15.1  # не работает на Python 3.12 (зависимость pyaes не собирается)
