# Дев-зависимости для тестов (не нужны в проде)
pytest>=8.0
pytest-asyncio>=0.23
# Держит tests/test_no_undefined_names.py — проверку на обращения к
# несуществующим именам (то же, что гоняет CI отдельным шагом).
ruff>=0.6
