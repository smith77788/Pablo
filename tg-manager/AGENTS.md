# AGENTS.md — Infragram (tg-manager)

Переписан под новый режим работы. Единый источник правил агента — **`CLAUDE.md`**
(режим работы + проектные факты + способы проверки). Этот файл — короткая точка
входа; не дублирует CLAUDE.md.

## Прежде чем писать код
1. Прочитай `CLAUDE.md` (режим работы и проектные факты).
2. Персистентной памяти/журнала/док-архива между сессиями нет — источник истины
   только код, тесты в репозитории и запрос владельца; проверяй их заново. Не
   создавай файл-«память»/журнал находок и не восстанавливай удалённый док-архив.

## Суть
- Infragram — Telegram-native инфраструктура и движок массовых действий.
  **Максимум возможностей Telegram. Минимум ручной работы.** Массовые операции —
  это продукт; всё важное со временем становится Operation.
- Не пересобирать с нуля, не ломать архитектуру, не удалять рабочую
  функциональность. Инспектировать файлы перед правкой.

## Границы
- Работать в `tg-manager/`, ветка `claude/telegram-bot-services-xfAh6`.
- Рантайм — Python 3.12-совместимый.
- Язык пользовательских сообщений и общения — только русский.
- Секреты — из git наружу не выносить (`.env`, `.state`, логи, кеши, venv локально).

Остальное — в `CLAUDE.md`.
