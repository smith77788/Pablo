-- schema_v167.sql — пол аккаунта (локальная категоризация для действий с аккаунтами)
--
-- Не поле Telegram-профиля (в API пола нет), а внутренняя пометка оператора:
-- по ней удобно назначать аккаунтам подходящие имена/аватары и фильтровать.
-- Значения: 'm' | 'f' | NULL. Каноничная копия инлайн-миграции (self-heal прода).

ALTER TABLE tg_accounts ADD COLUMN IF NOT EXISTS gender TEXT;
