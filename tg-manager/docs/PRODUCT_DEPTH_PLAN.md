# PRODUCT_DEPTH_PLAN.md — доводка продукта до «готового», а не MVP

**Статус: активный приоритет проекта.** Читать всем агентам перед работой над фичами.
Живой документ: punch-list ниже пополняется по мере аудита; сделанное — вычёркивать
(отметкой ✅ и коммитом), новое найденное — дописывать.

## Зачем это (диагноз)

Месяц несколько агентов наращивали ШИРИНУ (новые модули, плитки, экраны), но никто
не владел ГЛУБИНОЙ — «может ли живой пользователь пройти один ключевой путь от начала
до конца, ни разу не упершись в тупик или в тихий сбой». Итог: 98 плиток каталога,
400+ роутов — и при этом «Активен, но не работает», «Очищено 30», но не удалено,
сломанная кнопка «Создать», мёртвая «Карта связей», дубли-модули. Ощущение «сыро/MVP»
идёт НЕ от нехватки фич, а от того, что основным сценариям нельзя доверять.

## Метод (обязательный)

1. **Заморозка ширины.** Никаких новых модулей/плиток/экранов, пока живые не доведены.
   Исключение — только если новое прямо закрывает дыру в ключевом сценарии.
2. **Идти по пользовательским путям, не по фичам.** Берём ОДИН путь и доводим его до
   безупречного end-to-end: каждая кнопка работает, каждая нужная настройка на месте,
   каждое состояние даёт честную обратную связь, ноль тупиков и ноль «тихих успехов».
3. **Объективное «готово»** (см. `AGENT_PROTOCOL.md` → «Определение готово»): цепочка
   UI→route→handler→БД реально работает; нет мёртвых кнопок; действие даёт видимый
   результат; регресс-тест падает без фикса.
4. **Аудит перед доводкой.** Прежде чем чинить путь — пройти его насквозь и составить
   punch-list (файл/экран, что сломано/тупик/каких настроек не хватает, приоритет).

## Карта путей (порядок доводки)

1. **ЯДРО: аккаунт → операция** ← начинаем отсюда.
   Подключить аккаунт → прокси/IP-изоляция → прогрев → собрать аудиторию → запустить
   массовую операцию → увидеть реальный результат и здоровье аккаунтов.
2. **Контент → публикация.** Шаблоны/spintax → массовая публикация/быстрый пост →
   расписание → результат.
3. **Боты → аудитория → рассылка.** Создать/подключить бота → подписчики → воронки →
   рассылка → аналитика.
4. **Каналы.** Создание/управление → массовые действия → SEO/позиции.
5. **Наблюдаемость и настройки.** Дашборд как честное зеркало состояния + полнота
   настроек (глобальные, по-модульные, лимиты, прокси/IPv6, уведомления, тариф).

## Подозрения на недоработку (гипотезы — проверять по ходу, не факты)

Кросс-срезовые «сырые» сигналы, которые надо проверять на КАЖДОМ экране пути:
- **Пустые состояния.** Экран без данных объясняет, что делать дальше, или пугает
  пустотой/крутилкой? («нету информации» из скринов.)
- **Честная обратная связь.** После действия видно РЕАЛЬНЫЙ итог (успех/ошибка/почему),
  а не «поставлено в очередь» и тишина. Запрет «рапорт без эффекта».
- **Полнота настроек.** У модуля есть то, что реально нужно оператору (лимиты,
  расписание, фильтры, поведение), а не только «запустить». Это и есть «не хватает
  нужных настроек».
- **Нет тупиков.** С каждого экрана есть следующий логичный шаг; ошибки ведут к фиксу
  (релог, прокси, лимит), а не в никуда.
- **Загрузка/ошибки.** Есть внятные loading/error-состояния, не белый экран.
- **Консистентность.** Одинаковые названия/иконки для одного и того же; без дублей.
- **Мобильная вёрстка.** Это Telegram mini-app — узкий экран; без горизонтального
  скролла и схлопнутых кнопок.
- **Онбординг.** Новый пользователь понимает, с чего начать (первый аккаунт, первый
  прокси, первая операция).

Конкретные подозрения по ядру (проверить в аудите):
- Добавление аккаунта: понятно ли, КАК (бот/импорт сессии), и виден ли результат?
- Назначение прокси/IP-изоляции на аккаунт: доходит ли до эффекта, видит ли юзер?
- Прогрев: настройки достаточны? виден прогресс?
- Сборка аудитории → передача в операцию: связаны ли шаги, или это острова?
- Массовая операция: выбор целей, dry-run/канарейка, расписание, лимиты, живой
  прогресс, результат по каждой цели, ретрай упавших.
- Здоровье аккаунтов после операции: видно ли, что операция сделала с аккаунтами.

## Punch-list ядра «аккаунт → операция»

Формат: [ ]/[x] приоритет — экран/файл — проблема.

- [x] **P0 — Аккаунты: добавить аккаунт нельзя в мини-аппе.** Бэкенд-роут
  `/api/miniapp/import_sessions` существовал, но НИ одна кнопка его не звала; пустое
  состояние слало «Добавьте через меню в Telegram». Первый шаг ядра не работал в
  основном UI. → Добавлены кнопка «➕ Добавить» в шапке аккаунтов и actionable-пустое
  состояние → модалка импорта сессий (textarea + выбор прокси) → реальный итог
  imported/failed/errors → рефреш списка. Тест `test_account_import_ui`.
  - **Ре-аудит #1 (изоляция):** выбранный прокси использовался ТОЛЬКО для проверки,
    аккаунт добавлялся без proxy_id и падал на общий CF-relay (единый IP) — потеря
    изоляции с первого шага. → `import_sessions` принимает `proxy_id` (сверка на
    владельца), закрепляет его за аккаунтом в INSERT; модалка передаёт id+url.
  - **Ре-аудит #2 (до идеала, код+фронт):** (а) любой сбой валидации сваливался в
    «невалидная сессия» — мёртвый прокси выглядел как испорченная сессия, юзер мог
    удалить рабочую → причина КЛАССИФИЦИРУЕТСЯ (прокси/сеть vs недействительна vs
    флуд, reuse `classify_session_error`); (б) синхронная валидация N сессий (до 15с
    каждая) молча вешала запрос → порция ограничена (MAX_PER_IMPORT=20) с честным
    сообщением про остаток; (в) тест поймал реальный баг — сообщение об остатке
    срезалось `errors[:20]`, теперь вставляется вперёд. Тесты
    `test_session_import_quality` (3).
  - Итог: шаг 1 доведён до бара (код: owner-скоуп, классиф. ошибки, лимит,
    идемпотентность; фронт: кнопка/пусто/загрузка/честный итог/следующий шаг).

### Дальше по аудиту ядра (проверить след. проходами)
- [x] **P0 — Прокси/IP в карточке аккаунта: назначить можно, но НЕ видно текущий
  IP.** Карточка позволяла назначить прокси, но нигде не показывала, через какой
  прокси/IP аккаунт реально выходит (главный вопрос пользователя — «мой реальный
  IP»); модалка не предвыбирала текущий прокси → сохранение случайно снимало
  назначенный. → account_detail возвращает `transport` (свой прокси = уникальный IP
  1:1 с хостом; иначе IPv6, иначе общий CF-relay — честно «не уникальный»); карточка
  рисует строку «Прокси / IP» (тап → смена); модалка предвыбирает текущий прокси.
  Ре-аудит поймал edge-case: для admin межтенантного просмотра lookup по его uid
  давал бы неверный «общий IP» → транспорт считаем только для владельца, строку в
  admin-виде не рисуем. Тест `test_account_proxy_visibility` (3).
- [ ] Прогрев: запуск и настройки из карточки аккаунта — достаточно ли, виден ли прогресс?
- [~] **P0 — Аудитория → операция были острова.** После парсинга — тупик: только
  экспорт в файл и ручной перенос. → **DM-путь связан (готов):** на завершённом
  запуске парсера кнопка «📨 В рассылку» → композер предвыбирается на
  parsed_audience С parse_run_id этого запуска; submit шлёт target_id=run; backend
  и считает, и ОТПРАВЛЯЕТ по этому run (счётчик=факт, проверено по dm_engine).
  Тест `test_audience_to_operation_bridge` (4). Раньше композер всегда слал
  target_id=null → по ВСЕЙ аудитории.
  - [x] **Инвайтер-путь (симметрично DM, готов):** на запуске парсера кнопка «➕ В
    инвайт» → экран масс-инвайта с предвыбранным источником parsed и parse_run_id
    этого запуска; submit шлёт parse_run_id; mass_inviter_submit кладёт его в params;
    исполнитель (op_worker) фильтрует parsed_audiences `AND parse_run_id=$2` (иначе
    инвайт шёл бы по всей аудитории). Целевую группу пользователь вводит (это суть
    инвайта). Тесты в `test_audience_to_operation_bridge` (доп. 3).
  - Итог шага 3: остров связан для ОБОИХ путей (рассылка + инвайт) с точностью до
    запуска; счётчик=факт (DM), owner-scoped. Доведено до бара.
- [~] **P0 — Запуск массовой операции.** Аудит: живой прогресс (SSE), результат по
  каждой цели (operation_log), ретрай упавших (retry_operation), лимиты
  (delay/pace/max/per-acc) — УЖЕ есть. Острый пробел был в массовой публикации:
  «Опубликовать во все каналы» било СРАЗУ по всем, одним тапом, без подтверждения и
  без теста — необратимо.
  - [x] **Канарейка + подтверждение для mass_publish (готово):** поле «🐤 Тест на N
    каналов» → публикация в первые N (backend уже поддерживал channel_ids, UI не
    использовал); полная публикация во ВСЕ требует подтверждения (необратимо). Тест
    `test_mass_publish_canary` (4).
  - [ ] Меньший приоритет: канарейка для mass_invite (есть max_invites/per_account
    как троттл, но нет «тест на N целей»); явный пикер каналов (channel_ids готов).
- [x] **P0 — Здоровье аккаунтов после операции: не видно, что операция сделала.**
  Деталь операции не показывала след на аккаунтах; и глубже — `record_flood`
  ПРИНИМАЛ operation_id, но НЕ писал его в account_flood_log (параметр не доходил
  до эффекта) → флуд нельзя было связать с операцией. Фикс:
  - record_flood пишет operation_id (+ actual_wait, consecutive_count), фолбэк без
    v41-колонок; кулдаун-UPDATE вынесен из общего try с лог-INSERT (сбой лога не
    отменяет кулдаун → аккаунт не работает во флуд).
  - operation_status отдаёт accounts_impact (кто словил FloodWait за эту операцию,
    сколько раз, max wait, статус), owner-scoped, fail-soft.
  - Деталь операции: секция «📱 Влияние на аккаунты» (тап → карточка аккаунта).
  Петля «операция → её след на аккаунтах» замкнута. Тест `test_flood_op_impact` (4).

## ЯДРО «аккаунт → операция» пройдено (шаги 1–5)
Подключить аккаунт (в мини-аппе, с прокси) → прокси/IP виден и назначается →
аудитория из парсера сразу в рассылку/инвайт по запуску → массовая публикация с
канарейкой+подтверждением → влияние операции на аккаунты видно.

## Путь 2 «Контент → публикация» (в работе)

- [x] **P0 — Spintax НА КАНАЛ в массовой публикации (anti-detection).** Публикатор
  слал ОДИН текст во все каналы — палевная сигнатура координации. dm_engine уже
  спинтил per-recipient, публикатор — нет. → `_exec_mass_publish` раскрывает
  `expand_spintax(mp_text)` на каждый канал (оба send-пути), без spintax — no-op;
  подсказка в UI поля публикации. Тест `test_mass_publish_spintax` (3).
- [x] **Расписание массовой публикации (end-to-end, готово).** Инфра была готова
  (submit(scheduled_for), воркер уважает scheduled_for, cancel на pending), но
  mass_publish не принимал время И список операций не отдавал scheduled_for
  (запланированное было невидимым). Фикс: mass_publish принимает scheduled_for
  (UTC ISO, валидируется «в будущем»); operations/operation_status отдают
  scheduled_for; UI — поле datetime-local (локальное→UTC через toISOString),
  очередь и деталь показывают «⏰ запланировано на …» + отмену. Таймзона корректна
  сквозь всю цепочку. Тест `test_mass_publish_schedule` (4).
- [x] **Шаблоны → публикация (тупик устранён).** Экран «Шаблоны» был, но `useTpl`
  жёстко вставлял только в `bcastText` (сетевая рассылка) — пост из публикатора/
  кампании/быстрого поста нельзя было переиспользовать. Фикс: `openTemplates(target)`
  запоминает целевой композер (`TPL_TARGET`), `useTpl` пишет в него и обновляет
  счётчик; кнопка «📝 Вставить шаблон» у публикатора (`mpText`), DM-кампании
  (`cmpText`) и быстрого поста (`quickPostText`). Тест `test_template_into_publisher`.
- [x] **Quick post: ре-аудит.** spintax — уже покрыт (быстрый пост исполняется через
  `_exec_mass_publish`, где spintax на канал есть; добавлен guard-тест против форка
  без spintax). UI: добавлена подсказка про Spintax и кнопка вставки шаблона; поле
  расписания/повтора уже было. Тест `test_template_into_publisher` (guard +
  ui-hints).
- [x] **Anti-detection паттерн-свип (сиблинги mass_publish).** Ещё 4 аккаунт-рассылки
  слали ОДИН текст многим целям: `bulk_dm_adhoc` (ЛС), `group_announce`,
  `bulk_post_to_channel` (разные аккаунты в один канал), `bulk_post_chans`. Все →
  spintax на цель. Тест `test_broadcast_spintax_sweep` (4).

## Anti-detection: парные требования на КАЖДЫЙ аккаунт-исполнитель

Урок сессии: новый исполнитель по реальным аккаунтам обязан ставить **обе** защиты
разом — иначе дыра в ядре продукта:
1. **spintax на цель** (уникальный текст) — иначе палевная сигнатура координации;
2. **is_account_quarantined ПЕРЕД действием** (fail-open) — иначе флагнутый аккаунт
   идёт в бан;
3. **честный итог** — сколько целей/аккаунтов пропущено и почему (не «тихий» skip).

Свип выполнен по 4 текст-рассылкам (`bulk_dm_adhoc`, `group_announce`,
`bulk_post_to_channel`, `bulk_post_chans`): spintax + карантин + показ пропуска.
`dm_engine`/`mass_publish`/`invite`/`join`/`leave` уже уважали карантин. Инвайт получил
подтверждение перед запуском (самая баноопасная op).

Расширение на boost'ы: общий помощник `_filter_quarantined_accounts(pool, op_id,
accounts) → (kept, skipped)` (fail-open) применён к action-verb boost'ам
`boost_subscribers` (join) и `boost_bot_starts` (/start) + честный показ пропуска.
Пассивные boost'ы (views/reactions/stories) не гейтим осознанно (низкий риск).
`strike` НЕ трогаем: у него своя защита (is_strike_allowed, resource_selector
flood-aware, preflight cooldown/flood, warmup-guard) — второй фильтр был бы дублем.

Тесты: `test_broadcast_spintax_sweep`, `test_broadcast_quarantine_respect`,
`test_boost_quarantine_respect`, `test_mass_invite_confirm`.

## Уже закрыто в рамках доводки (этой линии)

- ✅ Аккаунты «активны, но не работают» → риск-пульс виден в списке/детали, причина
  сбоя (status_reason) человекочитаемо, само-heal истёкших кулдаунов.
- ✅ «Операция не найдена» → деталь по id; «Очистить»/«Пауза» честные (реальный
  DELETE / paused-статус, реальные счётчики); кривая кнопка «Создать» починена.
- ✅ Дубли-модули/дашборды и мёртвая «Карта связей» (дубль-хендлер) устранены.
- ✅ Нормализация ссылки источника парсинга (вставленный t.me/... резолвится).
