
## tg-manager: политика прокси — выбор strict/allow_direct + low_risk-операции без прокси — 2026-07-07
Запрос: (1) функции без массовых быстрых действий не должны гарантированно требовать прокси; (2) система должна давать выбор — работать только через прокси или разрешить без прокси (с риском блокировок). Плюс это вероятная причина, почему контакты не синхронизировались.
Найдено: `_resolve_client_proxy` жёстко поднимал `ProxyIsolationError`, если аккаунту назначен прокси, но URL пустой/битый — БЕЗ различия рисковости операции и без пользовательского выбора. Одиночное чтение (контакты) блокировалось так же, как массовое действие.
Сделано:
  - Чистый модуль `services/proxy_policy.py`: `proxy_decision(has_proxy_url, proxy_parsed_ok, policy, enforce, low_risk) → use|block|fallback`. Инварианты: валидный прокси → use; low_risk НИКОГДА не block (fallback на прямое/TG_PROXY); битый НАЗНАЧЕННЫЙ прокси при не-low_risk → block (не уводим прокси-аккаунт в сеть напрямую втихую — изоляция); нет прокси + strict/enforce → block, иначе fallback.
  - `_resolve_client_proxy(device, low_risk)` и `_make_client(session, device, low_risk=False)` — новый флаг (обратносовместим для всех 91 вызова). Глобальная политика: env `PROXY_POLICY` (default allow_direct) + per-owner override `device['proxy_policy']`.
  - Owner-политика: `db.get_proxy_policy(pool, owner)` читает `platform_users.settings_json.proxy_policy`; `get_account_for_telethon` докладывает `proxy_policy` в словарь аккаунта (теперь возвращает dict, +owner_id в SELECT). Эндпоинт settings уже сохраняет весь JSON.
  - `get_contacts` помечен `low_risk=True` → чтение контактов больше НЕ требует прокси: при allow_direct аккаунт с мёртвым/отсутствующим прокси уходит на прямое соединение и контакты синхронизируются (вероятный корень жалобы). Прямое соединение логируется (риск бана).
  - UI: в «Настройки → Безопасность» селектор «Политика прокси» (Только через прокси / Можно без прокси), грузится с бэка, сохраняется в settings_json.
Верификация: 1203 теста (+8 test_proxy_policy: все ветки решения, low_risk-инвариант, strict/allow_direct, enforce, normalize, проводка low_risk в get_contacts и proxy_policy в get_account_for_telethon; починен стаб _resolve_client_proxy в test_session_encryption под новую сигнатуру). Python AST + node JS чисто.
Осталось: прокинуть per-owner proxy_policy в массовый путь (resource_selector строит словари аккаунтов иначе — сейчас там действует процессная PROXY_POLICY/enforce, не per-owner); пометить остальные одиночные читающие операции low_risk (login_code/check_restriction идут через profile_setter_engine._connect — отдельный проход).

## tg-manager: политика прокси — проводка в массовый путь + low_risk для одиночных чтений — 2026-07-07
Добивка backlog предыдущей записи (выбор strict/allow_direct теперь действует и на массовых операциях; остальные одиночные чтения не требуют прокси).
Сделано:
  - Per-owner политика в массовом пути: `account_manager._OWNER_PROXY_POLICY` (process-local кэш, помечен «не переживает рестарт/не шарится между воркерами») + `set_owner_proxy_policy`. `_resolve_client_proxy` берёт политику по приоритету: явная в словаре аккаунта → кэш по `owner_id` → процессный дефолт (т.к. функция синхронна и сама в БД сходить не может). `resource_selector.select_all_active` теперь отдаёт `a.owner_id` (аддитивно, без риска для 10+ потребителей — лишняя колонка). op_worker праймит кэш на старте КАЖДОЙ операции из `db.get_proxy_policy`. Эндпоинт сохранения настроек тоже обновляет кэш немедленно.
  - Одиночные чтения без прокси: `_resolve_client_proxy` honors `device['_low_risk']` (чтобы не тащить low_risk через все сигнатуры движков). Инлайн-эндпоинты `login_code` и `check_restriction` помечают соединение `_low_risk=True` — как и контакты, они больше не требуют прокси. Массовый путь (_exec_bulk_set_profile тоже зовёт check_restriction) НЕ помечается → там прокси-политика в силе.
Ограничение (честно): кэш process-local — при нескольких воркерах каждый праймит свой на старте операции (операция целиком идёт в одном воркере, так что консистентно в пределах операции). Одиночные account-picks через flood_engine (select_account) owner_id в словарь не кладут → там действует процессный дефолт, не per-owner (одиночные, низкий риск; отдельный проход при необходимости).
Верификация: 1206 тестов (+3 к test_proxy_policy: проводка per-owner в массовый путь — кэш/owner_id в селекторе/прайм в op_worker; honor _low_risk + пометка инлайн-чтений). Python AST + node JS чисто.

## tg-manager: аудит правил №2-4 CLAUDE.md по всему tg-manager (кроме token_vault.py/account_manager.py/session_str-proxy_url зоны шифрования) — 2026-07-09
Метод: (п.2, опасные sync-хелперы) grep по всему дереву на `def (upsert_|sync_|import_all|replace_)` — 15 совпадений вне тестов; тело каждой прочитано целиком, для DB-функций — доп. grep `DELETE FROM` по всему проекту (исключая тесты) для функций с иным именем, но тем же паттерном. (п.3, потерянные параметры) прицельно не потребовалось отдельного прохода — искомый прецедент (`proxy_url` теряется по стеку) уже закрыт предыдущими сессиями (см. записи выше); при чтении audited-функций отдельно проверялось, что все объявленные параметры используются. (п.4, мультизначные/мультирежимные входы) искал match_mode/список-через-запятую паттерны — прецедент (`_match_rule`) уже закрыт и покрыт `tests/test_auto_responder_match.py`, новых необработанных ветвей match_mode/enum-like параметров не найдено вне зоны шифрования.
Найдено и исправлено (п.2): `database.db.upsert_managed_channels(pool, owner_id, acc_id, channels)` — единственная в проекте функция с паттерном "DELETE всех строк по ключу → INSERT". Вызывалась в 7 местах; в 6 из них передавался ЧАСТИЧНЫЙ список, из-за чего вызов тихо стирал остальные ранее сохранённые каналы аккаунта:
  - `bot/handlers/channel_factory.py::cb_chanf_do_create` — создание ОДНОГО нового канала персистилось списком из одного элемента → каждое создание канала стирало весь остальной список каналов этого аккаунта (самый severe случай, воспроизводится на каждом втором созданном канале при уже существующих каналах на аккаунте).
  - `bot/handlers/channel_factory.py::cb_chanf_import_acc`, `bot/handlers/group_factory.py::cb_group_import_acc`, `services/op_worker.py::_exec_channel_import_all`, `services/op_worker.py::_exec_group_import_all` — все четыре берут `account_manager.get_dialogs(limit=200)` (максимум 200 ДИАЛОГОВ, не 200 каналов/групп, без цикла по страницам) и передают отфильтрованный по типу список в `upsert_managed_channels`; у аккаунта с >200 диалогами суммарно часть каналов/групп не попадает в срез и стирается при каждом повторном импорте.
  - `services/op_worker.py::_exec_scan_owned_resources` — `to_import = owned[:slots_remaining]` осознанно обрезает список по остатку квоты подписки перед вызовом `upsert_managed_channels`; при повторном скане с меньшей квотой (понижение тарифа или квота уже выбрана другими аккаунтами раньше в этом же цикле) урезанный срез стирал уже сохранённые в БД каналы аккаунта, хотя они по-прежнему существуют в Telegram.
  Фикс: добавлена `database.db.add_managed_channels()` — точечный `INSERT ... ON CONFLICT (owner_id, channel_id) DO UPDATE` без предварительного DELETE; все 6 мест переключены на неё. `upsert_managed_channels()` оставлена как есть (с расширенным docstring, явно предупреждающим об опасности частичных данных) и продолжает использоваться в 2 местах, где вход — намеренно полный список: `bot/handlers/channel_ops.py::cb_manage_show_dialogs_live` и `_show_my_chans_page` (обе используют `account_manager.scan_owned_assets`, кнопка «Загрузить из Telegram» — пользователь явно просит пересобрать список admin/creator-каналов аккаунта с нуля, стейл-записи там должны удаляться; это осталось известным остаточным риском только в части собственного лимита `scan_owned_assets(limit=300)` на диалоги Telegram — это API-ограничение внутри `account_manager.py`, вне зоны этой сессии, не правилось).
  Отдельно проверено соседнее место `services/op_worker.py::_exec_channel_add` (вступление в канал по ссылке одним аккаунтом) — уже делало точечный `INSERT...ON CONFLICT` вручную с явным комментарием «НЕ upsert_managed_channels()»; баг там уже был закрыт кем-то ранее, тест на неизменность этого добавлен как контроль.
Ложные срабатывания при поиске: `services/graph_engine.py::upsert_node/upsert_edge`, `services/contacts_hub/crm_engine.py::upsert_crm`, `services/contacts_hub/repository.py::upsert_contact`, `services/contacts_hub/sync_service.py::sync_account/sync_all_accounts`, `services/semantic_memory.py::upsert_settings`, `database/db.py::upsert_users/upsert_user_activity/upsert_bot_admin_session/sync_plan_status_from_op`, `bot/utils/template_validator.py::replace_placeholders` — все прочитаны целиком, все либо уже точечный `INSERT...ON CONFLICT DO UPDATE`/`UPDATE-или-INSERT по существующей проверке`, либо (`replace_placeholders`) не БД-операция вовсе. Более широкий grep по `DELETE FROM` (без завязки на имя функции) по всему `database/db.py` и `services/` не выявил других full-resync-перед-partial-insert кандидатов — все прочие DELETE — явные, точечные, user-triggered удаления одной строки/набора по первичному ключу.
Тест: `tests/test_managed_channels_partial_import.py` (12 тестов) — поведенческие (fake pool: `upsert_managed_channels` по-прежнему делает DELETE, `add_managed_channels` никогда не делает DELETE и не трогает ранее вставленные строки при частичном довозе) + структурные (AST-разбор исходников файлов на предмет, что все 6 точек вызова используют `add_managed_channels`, а 2 намеренно оставленных — `upsert_managed_channels`, плюс контроль соседнего уже-корректного `_exec_channel_add`). Поведенческие тесты запущены через `asyncio.run()` вместо `@pytest.mark.asyncio` — в этом окружении `pytest-asyncio` не установлен (см. `docs/CLAUDE.md` п.5, известное отдельное ограничение окружения вне зоны этой сессии: тот же сбой воспроизводится на уже существующих `@pytest.mark.asyncio`-тестах по всему репозиторию, например `tests/test_contacts_hub_sync_and_search.py`). Структурные тесты изначально писались через `inspect.getsource(живой_объект)` по образцу существующих тестов и падали на функциях, обёрнутых `@router.callback_query(...)`, — `tests/conftest.py` подменяет такие функции стабом `_Any`, на котором `inspect.getsource` кидает `TypeError` (тот же сбой воспроизводится на уже существующем `tests/test_parser_commenters_wired.py::test_start_filter_includes_commenters` — не новая проблема, `conftest.py` не трогался). Переписаны на прямой разбор файла через `ast.parse` — не зависит от рантайм-стабов.
Верификация: `python3 -c "import ast; ast.parse(...)"` чисто на всех 4 изменённых `.py`-файлах + новом тесте; `python3 -m pytest tests/test_managed_channels_partial_import.py` — 12/12 зелёных; смежные `tests/test_channel_edit_ops.py`, `tests/test_op_worker.py`, `tests/test_operations.py`, `tests/test_executors.py`, `tests/test_core_services.py`, `tests/test_topology_and_channel_columns.py`, `tests/test_warmup_channels.py` — без регрессий (все зелёные).
Осталось (не в этой сессии, честно): `account_manager.scan_owned_assets(limit=300)` и `get_dialogs` — жёсткий cap на диалоги Telegram без пагинации по страницам; для очень больших аккаунтов (>300 диалогов) `upsert_managed_channels` в двух оставленных full-resync местах всё ещё теоретически может недосчитаться стейл-каналов на самой границе — фикс потребовал бы правки `account_manager.py`, которая явно вне периметра этой сессии (зона агента по шифрованию сессий).
